# RelayCore

RelayCore is a self-hosted AI Model Gateway for routing, billing, observability, and unified access across multiple upstream AI relay accounts.

It exposes OpenAI-compatible and Claude-compatible APIs, manages unified API keys, applies routing policies, tracks usage costs, handles upstream errors, and provides operational dashboards for model traffic.
